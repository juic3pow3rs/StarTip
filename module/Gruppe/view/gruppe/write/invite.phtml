<h1>Welchen Benutzer moechten Sie in Ihre Tippgemeinschaft einladen?</h1>
<?php
$form = $this->form;
$form->setAttribute('method', 'POST');
$form->setAttribute('action', $this->url('gruppe/invite', array(), true));
$form->prepare();

$form->get('submit')->setValue('Einladen');

echo $this->form()->openTag($form);

echo $this->formCollection($form);

echo $this->form()->closeTag();